Equations for constant motion, t is time

```
sin(t)

cos(t) * sin(t)

pow(sin(t), 3)

sin(t) * sin(t * 1.5)

abs(cos(t * 2) * (sin(t * 4)))

sin(tan(cos(t) * 1.2))

sin(tan(t) * 0.05)

cos(sin(t* 3)) * sin(t * 0.2)

sin(pow(8, sin(t)))

sin(exp(cos(t * 0.8)) * 2)

sin(t - PI * tan(t) * 0.01)

pow(sin(t * PI), 12)

cos(sin(t) * tan(t * PI) * PI / 8)

cos(sin(t * 3) + t * 3)

pow(abs(sin(t * 2)) * 0.6, sin(t * 2)) * 0.6
```
